{# templates/homepage/index.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-5">
        {% if user %}
            <h1 class="text-center">Bienvenue sur l'accueil, {{ user.firstName }}</h1>
        {% else %}
            <h1>Bienvenue sur l'accueil</h1>
        {% endif %}
    </div>
{% endblock %}