{% extends 'base.html.twig' %}

{% block title %}Devis{% endblock %}

{% block body %}
    <h1>Devis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ devi.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ devi.name }}</td>
            </tr>
            <tr>
                <th>CreateDate</th>
                <td>{{ devi.createDate ? devi.createDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Deadline</th>
                <td>{{ devi.deadline ? devi.deadline|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>TotalCost</th>
                <td>{{ devi.totalCost }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_devis_index') }}">back to list</a>

    <a href="{{ path('app_devis_edit', {'id': devi.id}) }}">edit</a>

    {{ include('devis/_delete_form.html.twig') }}
{% endblock %}
