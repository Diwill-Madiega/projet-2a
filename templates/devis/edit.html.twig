{% extends 'base.html.twig' %}

{% block title %}Modifier le devis{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4"><strong>Modifier le devis</strong></h1>

        <div class="card card-body">
            <div class="mt-4">
                {{ include('devis/_form.html.twig', {'form': form}) }}
                    <a href="{{ path('app_devis_index') }}" class="btn btn-secondary mt-2"><i class="fas fa-arrow-left"></i> Retour</a>
            </div>
        </div>
    </div>
{% endblock %}
