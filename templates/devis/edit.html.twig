{% extends 'base.html.twig' %}

{% block title %}Modifier le devis{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4"><strong>Modifier le devis</strong></h1>

        <div class="card">
            <div class="card-body">
                {{ include('devis/_form.html.twig', {'button_label': ' Enregistrer'}) }}
            </div>
        </div>

        <div class="mt-4">
        <a class="btn btn-secondary" href="{{ path('app_devis_index') }}"><i class="fas fa-arrow-left"></i> Retour</a>
        {{ include('devis/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
