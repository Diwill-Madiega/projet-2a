{% extends 'base.html.twig' %}

{% block title %}SellOrderLine{% endblock %}

{% block body %}
    <h1>SellOrderLine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sell_order_line.id }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ sell_order_line.amount }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ sell_order_line.price }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ sell_order_line.date ? sell_order_line.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Detail</th>
                <td>{{ sell_order_line.detail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sell_order_line_index') }}">back to list</a>

    <a href="{{ path('app_sell_order_line_edit', {'id': sell_order_line.id}) }}">edit</a>

    {{ include('sell_order_line/_delete_form.html.twig') }}
{% endblock %}
