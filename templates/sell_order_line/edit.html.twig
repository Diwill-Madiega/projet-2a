{% extends 'base.html.twig' %}

{% block title %}Modifier la ligne{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4"><strong>Modifier la ligne de commande de vente</strong></h1>

        <div class="card">
            <div class="card-body">
                {{ include('sell_order_line/_form.html.twig', {'button_label': ' Enregistrer'}) }}
            </div>
        </div>

        <div class="mt-4">
        <a class="btn btn-secondary" href="{{ path('app_sell_order_line_index') }}"><i class="fas fa-arrow-left"></i> Retour</a>
        {{ include('sell_order_line/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}