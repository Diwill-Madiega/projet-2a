<form method="post" action="{{ path('app_sell_order_line_delete', {'id': sell_order_line.id}) }}" class="d-inline" onsubmit="return confirm('Voulez-vous vraiment supprimer cette commande?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sell_order_line.id) }}">
    <button class="btn btn-danger"><i class="fas fa-trash"></i> Supprimer</button>
</form>