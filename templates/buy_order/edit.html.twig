{% extends 'base.html.twig' %}

{% block title %}Modifier la commande{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4"><strong>Modifier la commande d'achat</strong></h1>

        <div class="card">
            <div class="card-body">
                {{ include('buy_order/_form.html.twig', {'button_label': ' Enregistrer'}) }}
            </div>
        </div>

        <div class="mt-4">
        <a class="btn btn-secondary" href="{{ path('app_buy_order_index') }}"><i class="fas fa-arrow-left"></i> Retour</a>
        {{ include('buy_order/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}