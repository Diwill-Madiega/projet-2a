{% extends 'base.html.twig' %}

{% block title %}Modifier la commande{% endblock %}

{% block body %}
<div class="container">
    <h1 class="mt-4 mb-4"><strong>Modifier la commande d'achat</strong></h1>

    <div class="mt-4">
        {{ include('buy_order/_form.html.twig', {'form': form}) }}
        <a href="{{ path('app_buy_order_index') }}" class="btn btn-secondary mt-2"><i class="fas fa-arrow-left"></i> Retour</a>
    </div>
</div>
{% endblock %}