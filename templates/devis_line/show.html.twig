{% extends 'base.html.twig' %}

{% block title %}DevisLine{% endblock %}

{% block body %}
    <h1>DevisLine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ devis_line.id }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ devis_line.amount }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ devis_line.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_devis_line_index') }}">back to list</a>

    <a href="{{ path('app_devis_line_edit', {'id': devis_line.id}) }}">edit</a>

    {{ include('devis_line/_delete_form.html.twig') }}
{% endblock %}
