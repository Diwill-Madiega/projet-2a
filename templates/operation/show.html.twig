{% extends 'base.html.twig' %}

{% block title %}Operation{% endblock %}

{% block body %}

<div class="container mt-5">
    <h1><strong>Détails de l'opération</strong></h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ operation.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ operation.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ operation.description }}</td>
            </tr>
            <tr>
                <th>Durée (min)</th>
                <td>{{ operation.duration }}</td>
            </tr>
            <tr>
                <th>Poste</th>
                <td>{{ operation.post }}</td>
            </tr>
            <tr>
                <th>Machine</th>
                <td>{{ operation.machine }}</td>
            </tr>
        </tbody>
    </table>

        <div class="mt-4">
            <a href="{{ path('app_operation_index') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Retour
            </a>
            {% if user.getRightsRoleName() == 'responsable atelier' or user.getRightsRoleName() == 'administrateur' %}
            <a href="{{ path('app_operation_edit', {'id': operation.id}) }}" class="btn btn-primary">
                <i class="fas fa-edit"></i> Modifier
            </a>
            {{ include('operation/_delete_form.html.twig', {'class': 'd-inline'}) }}
            {% endif %}
        </div>
{% endblock %}
