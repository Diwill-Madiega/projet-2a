{% extends 'base.html.twig' %}

{% block title %}Nouvelle opération{% endblock %}

{% block body %}
<div class="container">
    <h1 class="mt-4 mb-4"><strong>Créer une opération</strong></h1>

    <div class="mt-4">
        {{ include('operation/_form.html.twig') }}
        <a href="{{ path('app_operation_index') }}" class="btn btn-secondary mt-2"><i class="fas fa-arrow-left"></i> Retour</a>
    </div>
</div>
{% endblock %}