{% extends 'base.html.twig' %}

{% block title %}Modifier l'opération{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4"><strong>Modifier l'opération</strong></h1>

        <div class="card">
            <div class="card-body">
                {{ include('operation/_form.html.twig', {'button_label': ' Enregistrer'}) }}
            </div>
        </div>

        <div class="mt-4">
        <a class="btn btn-secondary" href="{{ path('app_operation_index') }}" ><i class="fas fa-arrow-left"></i> Retour</a>
        {{ include('operation/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
