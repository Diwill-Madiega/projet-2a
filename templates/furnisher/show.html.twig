{% extends 'base.html.twig' %}

{% block title %}Fournisseur{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1><strong>Détails du fournisseur</strong></h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ furnisher.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ furnisher.name }}</td>
            </tr>
        </tbody>
    </table>

        <div class="mt-4">
            <a href="{{ path('app_furnisher_index') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Retour
            </a>
            <a href="{{ path('app_furnisher_edit', {'id': furnisher.id}) }}" class="btn btn-primary">
                <i class="fas fa-edit"></i> Modifier
            </a>
            {{ include('furnisher/_delete_form.html.twig', {'class': 'd-inline'}) }}
        </div>
</div>
{% endblock %}

