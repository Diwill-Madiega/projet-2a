{% extends 'base.html.twig' %}

{% block title %}Modifier la réalisation{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4"><strong>Modifier la réalisation</strong></h1>

        <div class="card">
            <div class="card-body">
                {{ include('production/_form.html.twig', {'button_label': ' Enregistrer'}) }}
            </div>
        </div>

        <div class="mt-4">
            <a href="{{ path('app_production_index') }}" class="btn btn-secondary"><i class="fas fa-arrow-left"></i> Retour</a>
            {{ include('production/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
