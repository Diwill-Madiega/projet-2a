<form method="post" action="{{ path('app_production_delete', {'id': production.id}) }}" class="d-inline" onsubmit="return confirm('Voulez-vous vraiment supprimer cette réalisation?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ production.id) }}">
    <button class="btn btn-danger"><i class="fas fa-trash"></i> Supprimer</button>
</form>
