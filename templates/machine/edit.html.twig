{% extends 'base.html.twig' %}

{% block title %}Modifier la machine{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4"><strong>Modifier la machine</strong></h1>

        <div class="card">
            <div class="card-body">
                {{ include('machine/_form.html.twig', {'button_label': ' Enregistrer'}) }}
            </div>
        </div>

        <div class="mt-4">
        <a class="btn btn-secondary" href="{{ path('app_machine_index') }}"><i class="fas fa-arrow-left"></i> Retour</a>
        {{ include('machine/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
