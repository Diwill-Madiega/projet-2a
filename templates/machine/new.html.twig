{% extends 'base.html.twig' %}

{% block title %}Nouvelle machine{% endblock %}

{% block body %}
<div class="container">
    <h1 class="mt-4 mb-4"><strong>Créer une machine</strong></h1>

    <div class="mt-4">
        {{ include('machine/_form.html.twig') }}
        <a href="{{ path('app_machine_index') }}" class="btn btn-secondary mt-2"><i class="fas fa-arrow-left"></i> Retour</a>
    </div>
</div>
{% endblock %}
