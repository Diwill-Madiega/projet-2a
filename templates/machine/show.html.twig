{% extends 'base.html.twig' %}

{% block title %}Machine{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1><strong>Détails de la machine</strong></h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ machine.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ machine.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ machine.description }}</td>
            </tr>
        </tbody>
    </table>

        <div class="mt-4">
            <a href="{{ path('app_machine_index') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Retour
            </a>
            {% if user.getRightsRoleName() == 'responsable atelier' or user.getRightsRoleName() == 'administrateur' %}
            <a href="{{ path('app_machine_edit', {'id': machine.id}) }}" class="btn btn-primary">
                <i class="fas fa-edit"></i> Modifier
            </a>
            {{ include('machine/_delete_form.html.twig', {'class': 'd-inline'}) }}
            {% endif %}
        </div>
</div>
{% endblock %}
