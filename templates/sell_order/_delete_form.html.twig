<form method="post" action="{{ path('app_sell_order_delete', {'id': sell_order.id}) }}" class="d-inline" onsubmit="return confirm('Voulez-vous vraiment supprimer cette commande?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sell_order.id) }}">
    <button class="btn btn-danger"><i class="fas fa-trash"></i> Supprimer</button>
</form>